/**
 * @author Hsieh,HoHui <billhsies@gmail.com>
 */
import React from 'react';
import AppContainer from '../components';

/**
 * The Home component serves as the main entry point for the application.
 * It renders the AppContainer component, which contains the core functionality.
 * 
 * @returns React.ReactNode - The rendered AppContainer component.
 */
export default function Home() {
    return (
        <AppContainer />
    )
}
